{
  "name": "@apex/desktop",
  "productName": "Apex",
  "version": "0.0.1",
  "private": true,
  "description": "Apex Desktop — Claude Code in Daytona sandboxes",
  "author": "Apex",
  "scripts": {
    "start": "npx electrobun build && npx electrobun dev",
    "build:dev": "npx electrobun build",
    "build:canary": "npx electrobun build --env canary",
    "build:stable": "npx electrobun build --env stable"
  },
  "dependencies": {
    "electrobun": "latest"
  }
}
