{
  "name": "@apex/api",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "bun build src/main.ts --target=bun --outdir=dist --sourcemap",
          "cwd": "apps/api"
        }
      },
      "serve": {
        "continuous": true,
        "executor": "nx:run-commands",
        "defaultConfiguration": "development",
        "options": {
          "command": "bun run --watch src/main.ts",
          "cwd": "apps/api"
        }
      }
    }
  },
  "dependencies": {
    "elysia": "*",
    "drizzle-orm": "*"
  }
}
